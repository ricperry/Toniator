#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=common.sh
source "$SCRIPT_DIR/common.sh"

(( $# >= 2 && $# <= 3 )) || die "usage: click X Y [BUTTON]"
run_vnc_helper click "$1" "$2" "${3:-1}"
