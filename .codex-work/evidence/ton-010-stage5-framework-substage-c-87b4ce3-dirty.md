# CACHE_UPDATE

- Current state: `87b4ce37d633181df485728cb903c4ff15b9470a` on `TON-010-Stage5-Framework-Restart`; dirty from retained Substages A/B and this C work. Preserved unrelated untracked files include `nextPrompt.md` and the pre-framework preservation evidence.
- C files/symbols: `resources/toniator-window.blp` adds the `weighted_voronoi` selector and `weighted_voronoi_panel`; `src/ui.rs` wires specialized controls through `DocumentEditor::set_weighted_voronoi_settings`, syncs them from `Document.pattern_state`, and adds realized selector/control coverage; `src/pattern.rs` has accurate specialized control metadata; `src/weighted_voronoi.rs` allocates positive and subtractive IDs from disjoint deterministic ranges and retains `WeightedVoronoiCellRelationship` as pairing metadata.
- Verified findings: the panel exposes semantic-channel enablement, exact cell count, shared/independent arrangement, source-weighted/uniform placement, density/interior-response, boundary gap, and an exact `u64` seed entry. UI edits route through the established autosave, preview invalidation, undoable editor path. The neutral placement/geometry owners remain unchanged. `cargo test --locked weighted_voronoi` passed all 5 focused adapter/model/preset/export-parity tests. `cargo fmt --check`, `cargo check --locked`, and `git diff --check` passed.
- Focused GTK result: the static Blueprint resource test passed. The realized GTK test was attempted and failed after the new Weighted Voronoi interaction leaves its fixture selected: `ui::tests::realized_numeric_controls_leave_continuous_scroll_to_parent` panics at `src/ui.rs:15315` because the subsequent Curves/crosshatch fixture assumes Curves remains selected (`ui.editing_context.text().starts_with("Curves · Crosshatch · Layer 1 (Black)")`). No broad follow-up edits were made after this failure.
- Commands: `cargo fmt --check`; `cargo check --locked`; `cargo test --locked weighted_voronoi`; `cargo test --locked editor_controls_resource_exposes_static_editor_structure_without_display`; `cargo test --locked ui::tests::realized_numeric_controls_leave_continuous_scroll_to_parent -- --exact` (failed as above); `git diff --check`.
- Artifacts: no screenshots or exports produced. The realized GTK test created and realized the resource window before its later assertion failure.
- Unresolved uncertainty: the focused GTK regression needs a narrow fixture reset back to Curves before the existing crosshatch assertion, then rerun. Also review the remaining negative non-adjacency assertion text containing `positive_region.0 + 1` in `src/weighted_voronoi.rs`; pairing itself no longer relies on adjacency, but the parent requested removal of adjacency-oriented assertions.
- Invalidation conditions: invalidate this evidence if `Document.pattern_state`/Weighted settings, semantic channel order, `DistributionLimits`, canonical region-ID allocation, Blueprint IDs, GTK synchronization callbacks, preview/export canonical routing, or the focused test fixture sequencing changes. Safe next handoff is the narrow test-fixture/acceptance cleanup only; do not begin broad docs or adapter redesign.
