# TON-010 bundled recipes — Substage 3D3A foundation parent review

- Repository/HEAD: `/home/ricperry1/projects/Toniator` at dirty `262c7e8`
- Date: 2026-08-01
- Status: reusable partial checkpoint, not 3D3A acceptance
- Producer: `desktop_implementer`; parent inspected orchestration, provider
  caching, layer assembly, initial equivalence, tests, and evidence.

## Accepted foundation

- A production-callable but non-dispatched Shapes recipe orchestrator requests
  semantic fields at recipe-declared lattice dimensions through the generic
  provider; equal dimensions share one resolution and distinct dimensions are
  resolved separately.
- Only enabled channels execute recipe/native work, while the assembled Marks
  output retains all stable output-model layers. Crosshatch color remains an
  external output-assignment concern.
- The first full canonical CMYK structure equals the retained compatibility
  oracle, including mark and layer order. Rotated coverage uses the configured
  theoretical maximum extent rather than source-observed values.
- Live document rendering remains compatibility=1 and recipe orchestration=0.

## Verification and remaining gate

- Parent Shapes-native suite passed: 9 tests; live non-switch proof passed.
- Parent format and diff checks passed. Writer previously reported 202 library
  and 48 binary/UI tests plus release/strict Clippy.
- 3D3A is not accepted until the full RGB/CMYK/assignment/alpha/primitive/
  custom-path/transform matrix, explicit disabled provider-zero assertion,
  deterministic cancellation coverage, and preview/PNG/editable-SVG consumer
  parity pass.

Invalidate when Shapes orchestration, semantic field provider/cache keys,
layer assembly, extent coverage, oracle renderer, HEAD, or dirty state changes.
