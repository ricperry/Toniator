# TON-010 Stage 5 Substage C implementation record

- Git assumption: started and stopped at `87b4ce37d633181df485728cb903c4ff15b9470a`, branch `TON-010-Stage5-Framework-Restart`, with retained dirty A/B work and unrelated untracked `nextPrompt.md`/preservation evidence. Nothing was reset, removed, committed, or pushed.
- Exact C files: `resources/toniator-window.blp`, `src/ui.rs`, `src/pattern.rs`, and `src/weighted_voronoi.rs`; two evidence records were added under `.codex-work/`.
- Implementation: added a scoped Weighted Voronoi selector and specialized Blueprint inspector; controls project from and write only `Document.pattern_state` through `DocumentEditor`. Existing autosave, undo, preview invalidation, cancellation/stale-result paths, canonical preview, PNG, and SVG routes remain reused. `WeightedVoronoiCellRelationship` remains the ownership record; region IDs now use disjoint deterministic positive/subtractive ranges rather than allocation adjacency.
- Metadata: expanded Weighted Voronoi `PatternParameterDescriptor` entries to match actual specialized persisted controls. This does not claim that the inspector is generic schema-driven UI.
- Verification: formatting, locked check, adapter/model/preset/export-focused tests, static resource test, and diff check passed. The realized GTK regression launched/realized controls but failed at the pre-existing Curves/crosshatch assertion after the new test interaction retained Weighted Voronoi selection; see the Substage C cache evidence for the exact assertion and command.
- Artifacts: no screenshots or export files. The realized test is the GTK runtime artifact and is currently failing.
- Known limitation/follow-up review: reset the realized test fixture to Curves before its crosshatch setup, rerun the exact GTK regression, and remove/reword the remaining negative `positive_region + 1` assertion so no adjacency-oriented assertion remains. Review any durable docs only after parent milestone review.
- Invalidation: revisit this record if pattern-state authority, channel order, distribution limits, canonical region IDs, Blueprint IDs, UI callback synchronization, canonical renderer/export route, or realized fixture ordering changes.
