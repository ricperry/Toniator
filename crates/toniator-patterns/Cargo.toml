[package]
name = "toniator-patterns"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
rayon.workspace = true
serde.workspace = true
toniator-domain = { path = "../toniator-domain" }
toniator-geometry = { path = "../toniator-geometry" }
toniator-sampling = { path = "../toniator-sampling" }

[lib]
path = "src/lib.rs"

[features]
# Compiles test-only observation records for engine-owned validation evidence. This feature is
# deliberately opt-in so production pattern consumers receive no observation API or ABI surface.
test-evidence = []

[dev-dependencies]
image.workspace = true
serde_json.workspace = true
